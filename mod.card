return {
  summary = "Optional battle information added to the existing native HUD.",
  author = "ishhodaszi",
  tags = { "ui", "quality-of-life", "cosmetic", "battle" },
  differences = {
    changed = {
      "Player name, status and HP share one clean alignment grid",
      "Player status sits left of level; opponent status sits to its right",
      "The native HP meters use clear green, yellow and red states",
      "The original player HUD grows upward for a dedicated EXP row",
    },
    added = {
      "A compact EXP mark and full-width one-pixel strip with current and required values",
      "The native Poké Ball tile directly beside the opponent name for caught species",
      "Gender Mod 0.3.5 alignment for the raised player level row",
      "One BATTLE INFO on/off setting in the standard Options menu",
    },
    known = {
      "Upstream Dramatic Shape, BATTLE ART VOXEL FORK 1.7.x and Dramaless Shape edit their original HUD texture before it is snapped",
      "Gender Mod keeps its own marker artwork and colour while Battle Info HUD aligns both its captured and post-pass markers",
      "Voxel HUD capture keeps Gender Mod pixels isolated from native name and panel pixels",
      "Battle Art 1.8+ seats Gender Mod beside its native staged level glyph instead of the name row",
      "Packaged mobile battles do not require Lua's optional debug library",
      "Text-only move-colour overlays keep the native TYPE/PP box clear of the EXP fill",
      "The existing HUD is enhanced in classic, WIDE and legacy staged layouts; Battle Art 1.8+ keeps its native staged pipeline",
      "Uses engine_internals to draw with Gen1Recomp's font and HUD tiles",
    },
  },
  credits = {
    { who = "Gen1Recomp", for_ = "native mod API and battle renderer" },
  },
  compat = { engine = ">=0.0.0-dev <1.0.0", modApi = 2 },
}
